function mudarCorTexto(obj, corTexto, corFundo) {
    obj.style.color = corTexto;
    obj.style.backgroundcolor = corFundo;
}

function pressEnter() {
    document.addEventListener('keypress',(event))
}